[/Script/EngineSettings.GameMapsSettings]
; GlobalDefaultGameMode is a C++ class, so it needs no Blueprint and applies to any
; level you open -- including an unsaved empty one. That is deliberate: it means the
; game is playable without a single .uasset existing.
GlobalDefaultGameMode=/Script/ChainOfWitnesses.ChainGameMode
; No default map yet: no level asset exists. Set GameDefaultMap and EditorStartupMap
; once a bootstrap level is saved -- see docs/SETUP.md.
