[/Script/EngineSettings.GameMapsSettings]
; No default map yet: this project is the simulation layer, and no level, pawn or
; widget asset exists. Set GameDefaultMap and EditorStartupMap once a bootstrap
; level exists -- see docs/SETUP.md.

[/Script/ChainOfWitnesses.ChainContentSettings]
; Written here by Project Settings -> Game -> Chain of Witnesses Content once the
; DataTables are imported and assigned. Left empty deliberately: the asset paths
; do not exist until the JSON in Content/Data is imported, and pointing at assets
; that are not there yet would only produce load errors on first open.
